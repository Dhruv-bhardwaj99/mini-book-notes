services:
  redis:
    image: redis:7
    container_name: mini-book-notes-redis
    ports:
      - "6380:6379"

  mongo:
    image: mongo:7
    container_name: mini-book-notes-mongo
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: